<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.com.example.goods">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.addCompanyActivity"></activity>
        <activity android:name=".activity.CompanyUpdateActivity" />
        <activity android:name=".activity.CompanyManageActivity" />
        <activity android:name=".activity.AdddePartmentActivity" />
        <activity android:name=".activity.DepartmentUpdateActivity" />
        <activity android:name=".activity.DepartmentManageActivity" />
        <activity android:name=".activity.UserUpdateActivity" />
        <activity android:name=".activity.UserManageActivity" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.MyInfoActivity" />
        <activity android:name=".activity.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GrantActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.MyActivity" />
        <activity android:name=".activity.CheckActivity" />
        <activity android:name=".activity.LoginActivity" />
    </application>

</manifest>